# Browser Teaspoon Debugging
To debug cql_qdm_patientapi tests in the browser, launch the dummy rails app
from the /test/dummy directory:

```
$ cd test/dummy
$ rails s
```

Then navigate to http://localhost:3000/teaspoon